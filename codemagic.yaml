# Codemagic CI/CD Configuration for ProCameraApp
# https://docs.codemagic.io/getting-started/yaml/

workflows:
  ios-release:
    name: iOS Release Build
    max_build_duration: 60
    environment:
      ios_signing:
        configuration: Release
      vars:
        BUNDLE_ID: com.procamera.app
    scripts:
      - name: Install dependencies
        script: |
          cd ios
          pod install --repo-update || echo "No CocoaPods, skipping"
      - name: Build iOS
        script: |
          cd ios
          xcodebuild \
            -project ProCameraApp.xcodeproj \
            -scheme ProCameraApp \
            -configuration Release \
            -sdk iphoneos \
            -archivePath $CM_BUILD_DIR/build/ProCameraApp.xcarchive \
            archive
      - name: Export IPA
        script: |
          cd ios
          xcodebuild \
            -exportArchive \
            -archivePath $CM_BUILD_DIR/build/ProCameraApp.xcarchive \
            -exportOptionsPlist ExportOptions.plist \
            -exportPath $CM_BUILD_DIR/output
    artifacts:
      - build/ios/output/*.ipa

  ios-debug:
    name: iOS Debug Build
    max_build_duration: 30
    environment:
      ios_signing:
        configuration: Debug
    scripts:
      - name: Build iOS Debug
        script: |
          cd ios
          xcodebuild \
            -project ProCameraApp.xcodeproj \
            -scheme ProCameraApp \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            build
    artifacts:
      - build/ios/iphonesimulator/Debug-iphonesimulator/*.app

  ios-test:
    name: iOS Tests
    max_build_duration: 45
    environment:
      ios_signing:
        configuration: Debug
    scripts:
      - name: Run Unit Tests
        script: |
          cd ios
          xcodebuild \
            -project ProCameraApp.xcodeproj \
            -scheme ProCameraApp \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            test
    artifacts:
      - build/ios/logs/*.xcresult

trigger:
  events:
    - push
    - pull_request
  branch_patterns:
    - pattern: 'main'
      include: true
      source: true
    - pattern: 'develop'
      include: true
      source: true
    - pattern: 'feature/*'
      include: true
      source: true
  tag_patterns:
    - pattern: 'v*'
      include: true

cache:
  cache_paths:
    - ~/Library/Caches/CocoaPods
    - ~/Library/Caches/com.apple.dt.Xcode
    - ios/Pods
